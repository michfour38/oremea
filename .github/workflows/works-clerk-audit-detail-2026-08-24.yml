name: One-time WORKS Clerk audit detail

on:
  push:
    branches:
      - works/security-clerk-5.7.6-2026-08-24
    paths:
      - .github/workflows/works-clerk-audit-detail-2026-08-24.yml

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Capture exact Clerk audit advisories
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm audit --omit=dev --json > /tmp/audit.json || true
          node <<'NODE' > .works-clerk-audit-detail
          const fs = require('fs');
          const audit = JSON.parse(fs.readFileSync('/tmp/audit.json','utf8'));
          for (const [name, vuln] of Object.entries(audit.vulnerabilities || {})) {
            if (!name.startsWith('@clerk/')) continue;
            console.log(`PACKAGE=${name}`);
            console.log(`severity=${vuln.severity}`);
            console.log(`range=${vuln.range}`);
            console.log(`nodes=${(vuln.nodes || []).join(',')}`);
            console.log(`fixAvailable=${JSON.stringify(vuln.fixAvailable)}`);
            for (const via of vuln.via || []) {
              if (typeof via === 'string') {
                console.log(`via_package=${via}`);
              } else {
                console.log(`via_source=${via.source}`);
                console.log(`via_title=${via.title}`);
                console.log(`via_url=${via.url}`);
                console.log(`via_range=${via.range}`);
                console.log(`via_severity=${via.severity}`);
              }
            }
            console.log('---');
          }
          NODE
          cat .works-clerk-audit-detail
          rm .github/workflows/works-clerk-audit-detail-2026-08-24.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .works-clerk-audit-detail .github/workflows/works-clerk-audit-detail-2026-08-24.yml
          git commit -m "security: record exact Clerk audit advisories"
          git push origin HEAD:works/security-clerk-5.7.6-2026-08-24
